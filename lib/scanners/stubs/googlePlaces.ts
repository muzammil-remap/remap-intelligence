// Phase 2 — Google Places API (reviews, rating, GBP completeness).
// Returns null until GOOGLE_API_KEY integration is built.
export async function runGooglePlaces(): Promise<null> {
  return null;
}
