// Phase 2 — Apollo.io company enrichment (headcount, revenue, industry, location).
// Returns null until APOLLO_API_KEY integration is built.
export async function runApollo(): Promise<null> {
  return null;
}
